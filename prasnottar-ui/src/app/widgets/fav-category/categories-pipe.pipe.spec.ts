import { CategoriesPipePipe } from './categories-pipe.pipe';

describe('CategoriesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
